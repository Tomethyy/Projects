# Companion mobile (Phase 4)

Planned: React Native or Flutter consuming the same OpenAPI as the employee portal.

This folder is reserved for the mobile client; backend endpoints are already versioned under `/api`.
