# SecPlan Excel import (optional seed)

Mapped template for dry-run import (`POST /api/import/secplan/dry-run`):

| Column | Field |
|--------|--------|
| A | PersonnelNumber |
| B | DisplayName |
| C | ExternalLegacyId (SecPlan reference) |

Row 1 = headers. Place sample exports from production here for **staging only** (no prod PII in git).
